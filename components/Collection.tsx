import React, { FC } from "react";
import styles from "../styles/collection.module.css";

const Collection: FC = () => {
  return <section className={styles.collection}></section>;
};

export default Collection;
